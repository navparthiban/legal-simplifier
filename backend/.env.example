# OpenRouter API key — get one at https://openrouter.ai/keys
OPENROUTER_KEY=

# Optional — port the backend listens on (default 8787)
PORT=8787

# Optional — sent to OpenRouter as the HTTP-Referer header
# (default http://localhost:5173)
APP_URL=
