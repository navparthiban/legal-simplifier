# Backend base URL. Leave blank for local dev (vite.config.ts proxies /api to
# http://localhost:8787). Set to your deployed backend's URL when building for
# production (e.g. on Netlify, as a site environment variable) — no trailing
# slash.
VITE_API_BASE=
